@model  RestaurantManagementProject.Models.Kitchen.KitchenViewModel


@{
    ViewBag.Title = "Index";


}
<div class="row">
    <div class="col-xs-12">
        <div class="table-responsive">

            <h1>Kitchen</h1>


            <h3>Open Orders</h3>
            <table class="table table-striped" title="Open Orders">

                <!-- Header-->
                <tr>
                    <th>Status</th>
                    <th>Time Created</th>
                    <th>Table ID</th>
                    <th>Mark Ready</th>
                </tr>
                @foreach (Order o in Model.OpenOrders)
                {
                    <tr>
                        <td>@o.State</td>
                        <td>@o.TimeCreated</td>
                        @if (o.Table != null)
                        {
                            <td>@o.Table.Id</td>
                        }
                        <td>
                            <a class="btn btn-primary" style="" href="@Url.Action("MarkOrderReady", "Kitchen", new { orderId = o.Id })">
                                <span class="glyphicon glyphicon-check"></span> Ready

                            </a>
                        </td>
                    </tr>

                }
            </table>

            <!--using (Html.BeginForm("MarkOrderReady", "Kitchen"))
            {
                <input type="number" id="ss" name="orderId" value="Enter Id" />

                    <input type="submit" value="Mark Ready" />
            }
                -->
            <h3>Ready Orders</h3>
            <table class="table table-striped" title="Ready Orders">
                <!-- Header-->
                <tr>
                    <th>Status</th>
                    <th>Time Created</th>
                    <th>Table ID</th>
                    <th>Mark Complete</th>
                </tr>
                @foreach (Order o in Model.ReadyOrders)
                {
                    <tr>
                        <td>@o.State</td>
                        <td>@o.TimeCreated</td>
                        @if (o.Table != null)
                        {
                            <td>@o.Table.Id</td>
                        }
                        <td>
                            <a class="btn btn-primary" style="" href="@Url.Action("MarkOrderReady", "Kitchen", new { orderId = o.Id })">
                                <span class="glyphicon glyphicon-check"></span> Complete
                            </a>
                        </td>
                    </tr>

                }
            </table>

        </div>
    </div>
</div>





<p> This should be a table of orders, listing name, price, table, status, etc.</p> </br>
<p> There will be a button on the right of each order that will allow you to edit the status of the order</p> </br>
<p>Status can be "Submitted", "Cooked", "Served", "Recall"</p> </br>
